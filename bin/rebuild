#!/bin/sh

# cabal clean && cabal configure && cabal build
# cabal clean && cabal configure --enable-executable-profiling --enable-library-profiling && cabal build
cabal clean && cabal configure --enable-tests && cabal build
